[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-utils"
version = "0.1.0"

[tool.setuptools]
packages = [
    "shared",
    "shared.utils"
]

[tool.setuptools.package-dir]
"shared" = "."
"shared.utils" = "utils"

